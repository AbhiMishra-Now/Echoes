import { Feather } from "lucide-react";
export function AIScribe({ onPrompt }: { onPrompt: (prompt: string) => void }) { const prompts = ["Tell me about your childhood...", "What is your greatest achievement?", "Share a memory that shaped you..."]; return <div className="ai-scribe"><div className="quill-orb"><Feather /></div><p className="eyebrow">YOUR PRIVATE CHRONICLER</p><h2>Your Royal Biographer Awaits</h2><p>I shall guide you through your life&apos;s story, one memory at a time. Speak freely, and I shall weave your tales into magic.</p><div className="prompt-grid">{prompts.map(p => <button key={p} onClick={() => onPrompt(p)}>{p}<span>↗</span></button>)}</div></div>; }
