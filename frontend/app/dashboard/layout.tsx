import type { Metadata } from "next";
export const metadata: Metadata = { title: "Your legacy | Echoes", description: "A private conversation with your AI biographer." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
